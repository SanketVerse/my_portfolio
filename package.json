{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Sanket Saraf Portfolio website",
  "scripts": {
    "build:css": "cleancss -o style.min.css style.css",
    "build:js": "terser script.js -o script.min.js --compress --mangle",
    "build": "npm run build:css && npm run build:js"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "terser": "^5.31.1"
  }
}
